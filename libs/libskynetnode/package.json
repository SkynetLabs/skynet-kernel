{
	"name": "libskynetnode",
	"version": "0.1.1",
	"description": "libksynet, but the non-isomorphic functions are written for node instead of the browser",
	"main": "dist/index.js",
	"type": "module",
	"types": "dist/index.d.js",
	"files": [
		"/dist"
	],
	"scripts": {
		"deps": "npm install libskynet@latest && npm install node-fetch@latest",
		"eslint": "eslint src --fix",
		"prettier": "prettier --write .",
		"lint": "npm run deps && npm run prettier && npm run eslint",
		"test": "tsc && tsc --project tsconfig.test.json && node ./dist-test/src-test/test.js",
		"prepare": "npm run lint && npm run test"
	},
	"author": "David Vorick",
	"devDependencies": {
		"@types/node": "^17.0.23",
		"@typescript-eslint/eslint-plugin": "^5.19.0",
		"eslint": "^8.13.0",
		"prettier": "^2.6.2"
	},
	"dependencies": {
		"libskynet": "^0.0.32",
		"node-fetch": "^3.2.6"
	}
}
